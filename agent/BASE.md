# Agent基本方針（BASE）

## 基本思想
- 正本はdocs/agent/INDEX.md
- ドキュメントは日本語を優先する

## 基本実装方針
- 特別に単純な指示でない限り、**実装の前に必ず設計を行うこと**。
- 実装方針は `plan.md` に更新すること。
- 更新した `plan.md` をユーザーに提示し、明示的な承認を得てから実装を開始すること。
- 承認を得る前に、コード作成・既存ファイルの変更・コマンド実行などの実装行為を開始してはならない。
